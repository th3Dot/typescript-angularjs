/// <reference path="../../_app.ts"/>

module App {
  'use strict';

  export class LoginCtrl {
    public static $inject = [
      '$scope'
    ];

    constructor(private $scope) {
      
    }
  }
}
